# Example environment configuration for backend
# Copy to .env and fill in real values. Do NOT commit the real .env.

# ── App ───────────────────────────────────────────────────────────
APP_NAME=Image-Gen Backend
ENV=development
DEBUG=true
HOST=0.0.0.0
PORT=8000

# CORS (comma-separated, no spaces)
CORS_ALLOW_ORIGINS=http://localhost:5173

# ── Gemini (Google Generative AI) ─────────────────────────────────
# Store your actual key in .env locally or as a CI/CD secret
GEMINI_API_KEY=your_gemini_api_key_here

# ── Object storage (S3-compatible) ────────────────────────────────
# MinIO configuration - matches docker-compose.yml
OBJECT_STORAGE_ENDPOINT=localhost:9000
OBJECT_STORAGE_BUCKET=ai-images
OBJECT_STORAGE_ACCESS_KEY=minioadmin
OBJECT_STORAGE_SECRET_KEY=minioadmin123
USE_HTTPS=false

# ── Logging ───────────────────────────────────────────────────────
LOG_LEVEL=INFO
STRUCTURED_LOGGING=true

